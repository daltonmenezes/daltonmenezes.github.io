import { styled } from 'styles'

export const Header = styled('header', {
  display: 'flex',
  flexFlow: 'column wrap',
  alignItems: 'center',
  justifyContent: 'center',
  margin: '0 1.5rem',
  gap: '5.2rem',
})
